"""Player-level probability models (Plan steps 5–10).

Deliberately empty until the market layer (steps 1–4) is reliable. Each model
starts life as a single module here (e.g. ``player_goals.py``) and is promoted
to a sub-package only when it needs more than one file — per the plan's own
guidance to avoid abstractions that only exist to match a folder tree.
"""
